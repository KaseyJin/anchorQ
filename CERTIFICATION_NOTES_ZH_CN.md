# AnchorQ Edge 商店认证说明

## 扩展用途

AnchorQ 用于 PDF 阅读期间的注意力管理与理解检验。扩展可以在用户准备访问其他网站时显示跳转前确认层，并可使用用户配置的 OpenAI 兼容模型，根据用户已经阅读的 PDF 文本生成问题和反馈。

## 无模型测试路径

审核人员无需配置模型即可测试以下功能：

1. 安装扩展，并在 Edge 的扩展详情页允许访问 `file://` 文件网址。
2. 打开 AnchorQ Side Panel。
3. 选择本地 PDF 并开始阅读。
4. 首次启用网页守护时，通过 Edge 官方权限界面允许访问普通 HTTP/HTTPS 网站。
5. 在学习会话期间尝试访问普通网站，确认目标页面正文出现前显示 AnchorQ 确认层。
6. 分别测试“返回论文”“继续使用”和“本次会话内不再询问”。

未配置模型时，阅读器、悬浮伴学和网页守护功能可正常使用；理解检查入口会明确说明需要配置模型。

## AI 功能测试凭据

以下字段仅在提交审核时填写到 Partner Center 的认证备注中，不得提交到源码仓库或打包进扩展：

- OpenAI 兼容 Base URL：`<审核专用 HTTPS 地址>`
- 模型名称：`<审核专用模型名称>`
- API Key：`<审核专用限额 Key>`
- 有效期：提交之日起 30 天（提交时填写 `<开始日期> 至 <失效日期>`）

审核专用凭据应满足：

- 与开发者日常凭据隔离；
- 设置明确的调用额度或消费上限；
- 只开放审核所需模型和接口；
- 可独立撤销；
- 审核完成后立即撤销；如审核尚未结束，则最迟在提交满 30 天时失效。

## AI 功能测试路径

1. 在 Side Panel 打开模型设置。
2. 填写审核专用 Base URL、模型名称和 API Key。
3. 阅读数据发送说明，勾选同意后点击“保存并测试”。
4. 打开一篇带文本层的 PDF，输入可选学习目标并开始学习。
5. 阅读若干页面后发起理解检查。
6. 回答模型生成的问题，确认系统给出基于已读内容的反馈。
7. 如果反馈包含原文依据，点击“查看论文证据”，确认阅读器跳转到对应页。
8. 在设置中点击清除凭据，确认 API Key 被移除。

## 数据与凭据说明

- API Key 仅保存在 `chrome.storage.session`，关闭浏览器后清除。
- Base URL 与模型名称保存在扩展本地存储中。
- PDF 文本仅在用户明确同意并发起理解检查时发送到用户配置的模型服务。
- 扩展不向 AnchorQ 开发者运营的服务器发送 PDF 内容、回答或 API Key。
- 当前版本只支持含文本层的 PDF，不支持对扫描图片执行 OCR。

## 受限页面

Edge 内部页面、扩展商店页面和其他浏览器禁止内容脚本运行的页面不受网页守护功能控制，这是浏览器平台限制。扩展不会尝试绕过该限制。

## 联系方式

- 发布者：kasey
- 支持邮箱：jinxy3799@gmail.com
- 隐私政策：https://kaseyjin.github.io/anchorQ/
