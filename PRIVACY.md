# AnchorQ 隐私说明（商店提交草案）

发布者：kasey

联系邮箱：jinxy3799@gmail.com

生效日期：2026 年 9 月 6 日

公开地址：https://kaseyjin.github.io/anchorQ/

## 本构建会处理的数据

- 当前学习会话的核心 PDF 地址与当前页码；
- 目标标签页的 URL、标题和规范化主机名；
- 用户在 AnchorQ Side Panel 中输入的学习目标、回答和后续问题；
- 为提出问题或核对回答所需的论文片段。

## 本构建不会处理的数据

- ChatGPT 对话内容；
- Bilibili 页面或视频内容；
- 其他普通网页正文；
- 输入框、密码或表单；
- Edge 之外的应用活动；
- 摄像头、麦克风或剪贴板。

## 不进行自动统计或错误上报

AnchorQ 第一版不集成使用统计、行为分析、广告追踪或崩溃上报服务，也不会在后台自动向发布者发送使用情况或错误信息。用户可以主动复制一份本地生成的脱敏诊断信息，其中只包含扩展版本、浏览器与平台、功能和权限状态，以及最近一次错误的操作类型、错误类别、HTTP 状态与时间；不包含 API Key、模型地址、模型名称、PDF 地址或标题、论文正文、学习目标、回答、对话或访问网址。诊断信息只有在用户自行发送时才会离开设备。

## 第三方模型请求与用户同意

AnchorQ 不内置公共模型账户。用户必须自行填写 OpenAI 兼容的 HTTPS 接口地址、模型名称和 API Key，并明确同意发送数据后，模型功能才会启用。

学习目标、问答内容以及完成辅导所必需的论文片段会直接发送给用户选择的模型服务。AnchorQ 当前不部署中转服务器，也不代表或控制第三方模型提供商。用户应自行查看该提供商的隐私政策、数据保留规则和费用标准。

## 存储与删除

- 角色、确认模式、模型接口地址和模型名称保存在 `chrome.storage.local`。
- API Key 仅保存在 `chrome.storage.session`，默认不向网页内容脚本开放，并在浏览器会话结束后清除。
- 活动会话、许可、挑战和对话保存在 `chrome.storage.session`。
- 用户结束会话后删除会话数据和对话，仅保留设置。
- 用户可随时在设置中清除模型凭据，或结束学习会话以删除当次对话。

## 权限用途

- `storage`：保存设置、临时凭据和当前学习会话。
- `tabs`：识别当前 PDF、返回核心材料，并在用户切换标签页时判断是否需要确认。
- `scripting` 与按需网页访问权限：仅在用户开始学习会话并通过 Edge 官方授权后，向普通网页安装分心访问确认层；会话结束后停止动态注册。AnchorQ 不读取普通网页正文、表单或密码。
- `sidePanel`：提供会话控制与伴学问答界面。

## 联系与更新

隐私或数据删除请求可发送至 `jinxy3799@gmail.com`。功能或数据处理方式发生变化时，本说明将同步更新。
